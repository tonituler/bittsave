import 'package:flutter/material.dart';

class Wallet2 extends StatelessWidget {
  const Wallet2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('wallet2')),
    );
  }
}
