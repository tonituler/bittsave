import 'package:flutter/material.dart';

class Wallet1 extends StatelessWidget {
  const Wallet1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('wallet1')),
    );
  }
}
